const pallete = {
    white: "#FFF",
    black: "#000"
}

export const colors = {
    common: {
        backgroundColor: pallete.white,
        shadowDefault: pallete.black,
    }
}

export type Colors = typeof colors;